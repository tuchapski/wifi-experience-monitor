services:
  postgres:
    image: postgres:17-alpine
    container_name: wifi-experience-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-wifi_experience}
      POSTGRES_USER: ${POSTGRES_USER:-wifi_experience}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-wifi-dev-password}
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"
    volumes:
      - wifi_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}",
        ]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  wifi_postgres_data:
